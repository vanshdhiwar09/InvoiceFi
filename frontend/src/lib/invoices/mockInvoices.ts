import { Invoice } from './types';

/**
 * Level 4 Production Invoice Dataset.
 * Dummy seed data removed for production release. Dashboard populates dynamically from Supabase & Soroban on-chain state.
 */
export const MOCK_INVOICES: Invoice[] = [];

